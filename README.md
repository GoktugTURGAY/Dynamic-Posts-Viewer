# Dynamic-Posts-Viewer
This project is a dynamic pagination application built with HTML, CSS, and JavaScript. It uses the DummyJSON API and the XMLHttpRequest (XHR) object to fetch and display posts dynamically. The project also includes pagination controls, loading states, error handling, and DOM manipulation for an interactive user experience.
